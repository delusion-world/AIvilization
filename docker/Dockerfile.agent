FROM python:3.12-slim

RUN pip install --no-cache-dir requests pandas numpy matplotlib
RUN useradd -m agent && mkdir /workspace && chown agent:agent /workspace

USER agent
WORKDIR /workspace

CMD ["tail", "-f", "/dev/null"]
